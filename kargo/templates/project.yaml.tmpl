apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: {{.project}}
spec:
  promotionPolicies:
{{- range .services}}
  {{- range $.environments}}
    - stage: {{.}}-{{.name}}
      autoPromotionEnabled: {{.autoPromote}}
  {{- end}}
{{- end}}
