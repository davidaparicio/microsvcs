apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: red
  namespace: microsvcs
spec:
  subscriptions:
    - image:
        repoURL: docker.io/davidaparicio/red
        semverConstraint: "*"
        discoveryLimit: 10
