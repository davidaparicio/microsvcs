apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: git-sync-releases
  namespace: microsvcs
spec:
  subscriptions:
  - image:
      repoURL: quay.io/davidaparicio/git-sync
      imageSelectionStrategy: SemVer
      semverConstraint: ">=0.0.0"
      discoveryLimit: 10
