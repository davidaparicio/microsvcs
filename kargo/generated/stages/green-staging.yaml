apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: green-staging
  namespace: microsvcs
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: green-releases
    sources:
      direct: true
  promotionTemplate:
    spec:
      vars:
      - name: image
        value: quay.io/davidaparicio/green
      steps:
      - uses: git-clone
        config:
          repoURL: https://github.com/davidaparicio/microsvcs.git
          checkout:
          - branch: main
            path: ./repo
      - uses: kustomize-set-image
        config:
          path: ./repo/k8s/overlays/staging/green
          images:
          # 'image' must match the 'name' field in kustomization.yaml (quay.io/davidaparicio/app)
          # 'newName' preserves the overlay's newName (quay.io/davidaparicio/green)
          - image: quay.io/davidaparicio/app
            newName: quay.io/davidaparicio/green
            tag: ${{ imageFrom(vars.image).Tag }}
      - uses: git-commit
        config:
          path: ./repo
          message: '[stg] green use ${{ imageFrom(vars.image).Tag }}'
      - uses: git-push
        config:
          path: ./repo
